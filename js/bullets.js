




        //bullet.reset(playerTankTurret.x, playerTankTurret.y);
        //var bullet = game.add.sprite(playerTank.x, playerTank.y, 'bullet');
        //bullet.physicsBodyType = Phaser.Physics.ARCADE;
        //bullet.enableBody = true;
        //bullet.anchor.x = -9;
        //bullet.anchor.y = 0.6;
        //.angle = playerTankTurret.angle;
        //game.physics.arcade.enable(bullet);
        //game.physics.arcade.velocityFromRotation(playerTankTurret.rotation, 800, bullet.body.velocity);   
    //}
  
    /*
    var nextFire = 0;
    var fireRate = 400;
    
    if(game.time.now > nextFire && playerBullet.countDead() > 0) {
        nextFire = game.time.now + fireRate;
        
        var bullet = playerBullet.getFirstDead();
        bullet.angle = playerTankTurret.angle;

        bullet.reset(playerTankTurret.x, playerTankTurret.y);
        game.physics.arcade.enable(bullet);
        game.physics.arcade.velocityFromRotation(playerTankTurret.rotation, 800, bullet.body.velocity); 
        
    }
    */